# CNF file path
/ECE6745_Final_Project/Kallas_example/ABC/example.cnf

# Example function
function: e = a * b * c = (a * b) * c 
d = (a * b)
e = d * c

# Solving for the number of clauses and literals
d  <=>  a*b	=> (~d + a)(~d + b)(~a + ~b + d)
e  <=>  d*c	=> (~e + d)(~e + c)(~d + ~c + e)

5 literals
6 clauses

# Run picosat
../../tools/picosat-965/picosat example.cnf
